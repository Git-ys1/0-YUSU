# 下一步

## 推荐后续工作

1. 给 `KeyClicker` 的按键解析和轮流/同时模式补一个正式测试文件，把临时 monkeypatch 冒烟测试固化下来。
2. 如果恢复厨房图像识别模式，应把它作为独立模式重建，提供可见状态机、区域/模板预览、每个菜品独立备菜逻辑；不要混进宏录制器控制面板。
3. 考虑把无关下载工具（`MaaNTE`、`NTE-Piano-Player-v1.5.2-fixed`）移出 Auto Play 根目录，或明确记录为外部研究资产。
4. 如果这个工具继续演进，给 `F:\Project\auto play` 初始化 Git；当前没有 Git 历史会限制后续证据质量。
5. 如果用户想跨机器使用，增加宏和设置的导出/导入包。
