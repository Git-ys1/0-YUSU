# Todo Next

## Next Actions

- [ ] On Ubuntu 20.04, clone `https://github.com/Git-ys1/0-YUSU.git`.
- [ ] Run `bash tools/setup-codex-endpoint.sh`.
- [ ] Verify Ubuntu Codex can read `YUSU_KB_ROOT` and discover `yusu-kb`.
- [ ] Make a tiny Ubuntu-side test commit, push it, then pull it on Windows.
- [ ] Run mature-project retrospective audit for this vault after the 2026-06-09 mature files are written.
- [ ] Review older project entries that predate super-yusuV0.2 and decide which need routing-audit upgrades.

## Needs User Decision

- [ ] Decide whether direct NTFS shared folder is still worth setting up after GitHub sync is working.
- [ ] Decide whether to install Obsidian Smart Connections later for human-side semantic search.

## Later

- [ ] Consider a local MCP semantic search server only after the vault grows beyond keyword-search comfort.
- [ ] Add a periodic check that Marginalia derived indexes are stale after Markdown commits.
