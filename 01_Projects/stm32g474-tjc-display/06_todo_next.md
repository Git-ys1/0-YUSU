# Todo Next

## 2026-09-20赛后剩余工作

- 比赛功能开发已结束；下面V2.6/V2.7/V2.8条目均视为历史待办，不再默认执行。
- 当前仅剩仓库卫生工作：按主题审计本地脏工作树，决定哪些赛末文档、交接包和
  代码变化应提交，哪些应归档或忽略；完成前禁止一次性`git add -A`发布。
- 如需复现实赛，从`teammate/24`复制到临时目录构建，不直接修改冻结真源。
- 外部资料目录`24`暂保留为原始证据；是否删除副本需要用户单独授权和备份确认。
- 前端复频响、双ADC失配和模型残差仍缺完整实测数据，但比赛已经结束，统一记录为
  “历史未闭环验证”，除非项目转为后续科研，不再作为默认P0。

以下内容为赛前历史，不代表当前要继续开发。

## V2.8新模式8192点升级

- 等队友提供固定新模式、8192点`getup()`工程；融合时保持ADC/FFT 4096点不变。
- 将`AnalyzerBridge_SetPendingPhaseModelWaveform()`输入契约改为8192，并把内部
  显示波形从256点提高到512点，匹配淘晶驰时域曲线512像素宽度。
- 用DWT或GPIO翻转实测`getup()`从4096增至8192后的耗时，确认采集停顿可接受。

## V2.7当前P0（2026-08-01）

- 队友先按`deliverables/V2.7更新包/V2.7更新摘要.md`覆盖四文件并手工修改
  `main.c`三处；不得用我方完整`main.c`覆盖其ADC、FFT、Goertzel和KEY逻辑。
- Clean Rebuild后先验收Upp=`getup()`、Urms=`Vr`、4096点理论时域、最终FFT频谱，
  并确认Mpp显示无效占位、旧模式帧被拒绝。
- 实测刷新按钮到两条曲线及文本更新是否小于2秒；若超时，记录两条`addt`各自的
  FE/FD响应，不要把3秒自动刷新周期当作手动刷新延迟。
- 同步保留信号源、示波器、ADC1/ADC2各2048点、交错VO、两次FFT元数据、Goertzel
  相位和屏幕结果，作为理论解析曲线的闭环证据。
- 我方测试板已按用户指令烧录；队友反馈与本机实测通过前不提交、不发布V2.7。
  反馈回来后再决定是否优化两次FFT、第三次无效Goertzel和`getup()`三角计算热点。

## V2.6当前P0（2026-08-01）

- 当前发布基线是`v2.6.0`/`4894940`，队友底座为原`win`、现
  `teammate/current/`；正式固件只从`firmware/`构建。
- 用户先在dashboard加入全局状态开关`sw_model`，默认0，按
  `docs/01_architecture/HMI_RECONSTRUCTION_MODE_SWITCH.md`设置弹起和页面初始化
  事件；完成前不要烧录当前未发布HEX。
- HMI完成后烧录并验证三态：旧普通、旧Huber、新幅相解析；新模式期间误触
  `sw_huber`不得改变MCU状态，返回旧模式后再切Huber才生效。
- 取得前端完整复频响原始扫频表：至少记录频率、输入参考、ADC1/ADC2复系数、
  VDDA、温度和量程；先保留原始表，再做分段线性插值和留出点残差验证。
- 让同一信号同时进入PA0/PA1，同帧导出两路2048点数组、`VO[4096]`、Goertzel
  相位、解析曲线、Huber曲线、示波器和信号源参考，建立模型残差门控。
- 实测并统一2048000与2048193 Hz采样率口径，加入亚bin频率细化后再验相位。
- `Vpp_Robust()`已经退出运行链；后续可在队友确认后删除历史源码，但不要恢复调用。
- 按用户决定继续暂停独立4.1 MS/s高速ADC实验；没有新指令时不要迁入主线。

以下条目为历史待办；与上述V2.5状态冲突时以上述内容为准。

- 给独立`experiments/g474_high_speed_adc/`烧录前先确认连接的是备用G474板而非
  当前主线显示板；用共地、1.65 V偏置、1.0 Vpp、100 kHz正弦从PA0开始。
- 导出`g_adc_samples[2048]`并记录`g_adc_status`；用示波器/定时器核对约
  4.100 MS/s、ADC1/ADC2交错顺序、码值范围和双ADC增益/偏置失配。
- 高速采样实测通过后，再按题目真正需要保留的最高基波/谐波重设计FIR；不要直接
  把作者约691 kHz截止的31抽头参数放进正式主线。
- 2026-08-01成熟项目只读回顾审计仍有27项历史结构缺口：缺少
  `00/01/02/07/08/09`六份成熟项目文档，旧`03/05/10`标题也未满足新审计器模式；
  本轮已完成项目进度、风险、跨项目坑点和Memory Routing Audit更新，但完整旧会话
  考古应另开专门入库任务，不在高速ADC实验中临时补写。

- 队友发回其已经融合V2.2后的最新工程时，以其工程为底座，只对比并合入
  `deliverables/V2.4更新包/`中的`analyzer_bridge.c/.h`和`display.c`；不得
  用我方`main.c`覆盖队友的新采集与按键逻辑。
- 新会话首先阅读仓库`docs/HANDOFF/README.md`及知识库
  `11_current_handoff_2026-07-31.md`；当前主线已经进入V1.9.0真实ADC联调，
  不再重做显示封装、双页面或旧曲线ID。
- P0是获取同一稳定状态下的信号源设置、PA0示波器、`adc_b[2048]`和屏幕结果；
  用户已确认前两者Vpp通常差不到5 mV，但示波器与屏幕经常差超过10 mV。
- 原始数组离线比较`max-min`、当前10点极值均值、分位数、正弦/谐波最小二乘
  Vpp，并实测VDDA；在此之前不提交显示比例或平滑补丁。
- 对纯正弦的第二峰增加存在性审计：绝对阈值、相对阈值、噪声底和整数倍关系；
  现有队友FFT至少寻找两峰，不能把所有剩余峰直接标成谐波。
- 给相位折叠增加基波正向过零相位锚，并用独立相位数、覆盖率、最大空洞、
  频率稳定性和拟合残差联合决定折叠/降槽/模型重建，不得只看覆盖率。
- 后续代码工作默认从`F:\Project\stm32G474VETx\TI\G_Periodic_Signal_Analyzer`进入，不再直接修改`test/`中的原始副本。
- V1.4融合固件已完成烧录与HMI模拟器稳定回归；后续从冻结提交继续，不再重复融合或重构显示主链。
- 保留`tjc_display_demo`的V1.3.1 100 kHz演示固件作为独立显示回归基线；融合工程出现问题时先区分显示基线与队友算法问题。
- 与信号处理队友确认V1.4当前“最低有效谱峰即基频”的假设是否覆盖全部题目输入，并确认队友最终接口是否会继续调整。
- 审查新快照`fft()`清零峰值附近正负8个频点时的数组上下界，以及2分量场景下`F`是否仍可能停留在最强峰而非基频。
- 修正或重新选择采样率/FFT长度，使实际频率分辨率不大于500 Hz，并使用实际定时器采样率参与计算。
- 将正式单页面`testv2.HMI`及编译后的屏幕资源纳入显示工程，按版本和固件成对归档；旧`testv1.HMI`不得用于V1.3.1。
- 已在当前不可触摸的淘晶驰X2 7英寸实体屏上完成V1.4.1 KEY1回归：上电、1T、3T、测试、频谱和六项文本正常。
- 若后续更换回可触摸屏，继续保留KEY1与HMI共用`Display_ProcessButtonCommand()`的结构，只通过宏决定是否启用实体键。
- 继续保持收到dashboard 6字节初始化帧、保存两个真实曲线ID后再绘图。
- 用真实信号源覆盖10至500 kHz和50至250 mVpp范围，建立前端增益、偏置和频响校准表。
- 将`ANALYZER_TEST_ENABLE`设为0做正式构建回归，并保留测试版HEX作为显示链路诊断固件。
- 使用`tools/custom_waveform_lab.py`补充评测边界组，保存后重新导出固件头文件；需要上板时才临时启用`ANALYZER_CUSTOM_TEST_ENABLE=1`，验收后恢复0。
- 对128、160、200、256、341.333 kHz等低相位覆盖点决定正式策略：可切换采样率优先，其次验证谐波最小二乘；没有稳定触发前不承诺顺序等效时间采样。
- 用真实信号源在256 kHz及其±500 Hz邻域验证频率估计跨帧稳定性、相位折叠残差和谐波LS残差。
- 若把自适应槽数移植到STM32，先实现“覆盖几何+样本残差+矩阵秩/条件数”联合门控，禁止只按计算覆盖率硬切换。
- 实机回归V1.8时域方向修复，确认T102相位0后走势与PC实验室一致；确认后再恢复正式测试宏。
- 后续真实ADC联调从V1.9.0继续：PA0/ADC1_IN1输入，先核对偏置、Vpp、RMS、FFT频率和幅值，再讨论算法升级。
- 用示波器或定时器实测ADC1采样率，统一FFT内部`1024000`与桥接/Goertzel`1024090`的口径。
- 对同一组标准正弦和谐波输入同时记录FFT RSS与Goertzel输出，完成标定后再决定正式发布源。
- 检查队友FFT峰附近±8频点处理在频带低端和高端的数组边界。
- 队友再次发工程时先独立归档原始快照，再把CubeMX底座同步到V1.9融合层；不得从V1.4/V1.8重新融合。
- 用户按V2.4定稿保存HMI后归档到`firmware/HMI/`，核对512×256时域、256×256
  频谱、`n_ty*`、`n_sy*`、`x_tx*`和`sw_period`对象名。
- 烧录V2.4.0后用KEY1执行1T/3T往返，确认`sw_period`外观和时域us刻度同步，
  串口无`0x1A`对象名错误，`addt`长度分别为512和256。
- 同步记录信号源、PA0示波器、同帧`adc_b[2048]`、屏幕`Upp/Mpp`和VDDA，
  再决定最终峰峰值口径；当前不修改或启用队友`Vpp_Robust()`替代方案。
